# Task Management API

REST API для управления задачами, построенная на FastAPI с использованием Clean Architecture принципов.

## 🚀 Технологический стек

- **FastAPI** - современный асинхронный веб-фреймворк для Python
- **SQLAlchemy 2.0** - ORM с поддержкой асинхронных операций
- **PostgreSQL** - реляционная база данных
- **Alembic** - инструмент для миграций базы данных
- **Pydantic v2** - валидация данных и настройки
- **Pytest** - фреймворк для тестирования
- **Docker & Docker Compose** - контейнеризация приложения
- **Ruff** - быстрый линтер и форматтер для Python

## 📁 Архитектура проекта

Проект следует принципам Clean Architecture с разделением на слои:

```
app/
├── api/              # API эндпоинты (FastAPI routes)
│   └── v1/          # Версионирование API
├── core/            # Ядро приложения (конфигурация, исключения, безопасность)
├── db/              # Настройка базы данных
├── models/          # SQLAlchemy модели
├── repositories/    # Слой доступа к данным (Repository pattern)
├── schemas/         # Pydantic схемы для валидации
└── services/        # Бизнес-логика приложения
```

## 🛠️ Установка и запуск

### Предварительные требования

- Python 3.11+
- PostgreSQL 15+
- Docker и Docker Compose (опционально)

### Локальная установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd task_manager
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
pip install -r requirements-dev.txt
```

4. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

5. Настройте переменные окружения в `.env`:
```env
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/task_manager
SECRET_KEY=your-secret-key-change-in-production
```

6. Запустите миграции Alembic:
```bash
alembic upgrade head
```

7. Запустите приложение:
```bash
uvicorn app.main:app --reload
```

Приложение будет доступно по адресу: http://localhost:8000

### Запуск с Docker(рекомендуется)

1. Создайте файл `.env` (см. выше)

2. Запустите контейнеры:
```bash
docker-compose up -d
```

3. Выполните миграции:
```bash
docker-compose exec api alembic upgrade head
```

Приложение будет доступно по адресу: http://localhost:8000

## 📚 API Документация

После запуска приложения доступна интерактивная документация:

- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

## 🔐 Аутентификация

API использует JWT токены для аутентификации. Для получения токена:

1. Зарегистрируйте пользователя: `POST /api/v1/auth/register`
2. Войдите в систему: `POST /api/v1/auth/login`
3. Используйте токен в заголовке: `Authorization: Bearer <token>`

## 📝 Основные эндпоинты

### Аутентификация

- `POST /api/v1/auth/register` - Регистрация нового пользователя
- `POST /api/v1/auth/login` - Вход в систему
- `GET /api/v1/auth/me` - Получить информацию о текущем пользователе

### Задачи

- `POST /api/v1/tasks` - Создать задачу
- `GET /api/v1/tasks` - Получить список задач
- `GET /api/v1/tasks/{task_id}` - Получить задачу по ID
- `PUT /api/v1/tasks/{task_id}` - Обновить задачу
- `DELETE /api/v1/tasks/{task_id}` - Удалить задачу

## 🧪 Тестирование

Тестам нужна PostgreSQL с отдельной базой `task_manager_test`.

**Вариант 1 — тесты в Docker (рекомендуется):** поднимаем контейнеры, один раз создаём тестовую базу, затем запускаем тесты в контейнере API:
```bash
docker-compose up -d
docker-compose exec db psql -U postgres -c "CREATE DATABASE task_manager_test;"
make test-docker
# или: docker-compose exec api pytest -v
```

**Вариант 2 — тесты локально:** нужен доступ к PostgreSQL (локально или порт Docker 5432). В `.env` укажи `DATABASE_URL` с тем же хостом/паролем; тестовая база будет подставлена как `task_manager_test`. Либо задай `TEST_DATABASE_URL` явно.
```bash
pytest
```

С покрытием кода:
```bash
pytest --cov=app --cov-report=html
```

## 🔍 Линтинг и форматирование

Проверка кода с помощью Ruff:
```bash
ruff check .
```

Форматирование кода:
```bash
ruff format .
```

Или используйте Makefile:
```bash
make lint
make format
```

## 📋 Makefile команды

```bash
make install      # Установить зависимости
make run          # Запустить приложение
make test         # Запустить тесты
make lint         # Проверить код линтером
make format       # Отформатировать код
make docker-up    # Запустить Docker контейнеры
make docker-down  # Остановить Docker контейнеры
make migrate      # Применить миграции
make clean        # Очистить временные файлы
```

## 🗄️ Миграции базы данных

Создать новую миграцию:
```bash
alembic revision --autogenerate -m "description"
```

Применить миграции:
```bash
alembic upgrade head
```

Откатить миграцию:
```bash
alembic downgrade -1
```

## 🏗️ Структура базы данных

### Таблица `users`
- `id` - первичный ключ
- `email` - уникальный email
- `username` - уникальное имя пользователя
- `hashed_password` - хешированный пароль
- `is_active` - статус активности
- `created_at` - дата создания
- `updated_at` - дата обновления

### Таблица `tasks`
- `id` - первичный ключ
- `title` - заголовок задачи
- `description` - описание задачи
- `is_completed` - статус выполнения
- `user_id` - внешний ключ на users.id
- `created_at` - дата создания
- `updated_at` - дата обновления

## 🔒 Безопасность

- Пароли хешируются с использованием bcrypt
- JWT токены для аутентификации
- Валидация данных через Pydantic
- Глобальная обработка ошибок
- Защита от SQL-инъекций через SQLAlchemy

## 📝 Логирование

Приложение использует структурированное логирование через `structlog`. Логи выводятся в консоль в режиме разработки и в JSON формате в продакшене.
